{
  "name": "wiki",
  "version": "0.0.0",
  "description": "Knowledge + delivery system over a locked Obsidian vault. Single CLI is the only writer.",
  "type": "module",
  "bin": {
    "wiki": "./dist/cli.js"
  },
  "scripts": {
    "dev": "bun --watch src/cli.ts",
    "build": "bun build src/cli.ts --outdir dist --target bun",
    "test": "bun test tests/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "smol-toml": "1.6.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
